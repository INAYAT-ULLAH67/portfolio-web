{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd tecsec && python manage.py migrate && gunicorn tecsec.wsgi:application --bind 0.0.0.0:$PORT"
  }
}